# SQL_introduction

This project covers the basics of SQL and MySQL: creating and deleting databases, creating tables, inserting and updating records, and querying data with SELECT, WHERE, GROUP BY, and ORDER BY.
