# SQL_more_queries

This project covers more advanced MySQL: creating users and managing privileges (GRANT/SHOW GRANTS), column constraints (NOT NULL, UNIQUE, DEFAULT), primary and foreign keys, and querying multiple tables with JOIN and subqueries.
